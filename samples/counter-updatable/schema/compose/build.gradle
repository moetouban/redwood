apply plugin: 'org.jetbrains.kotlin.multiplatform'
apply plugin: 'app.cash.treehouse.schema.compose'

kotlin {
  js {
    browser()
  }
  jvm()
}

treehouseSchema {
  source = project(':samples:counter-updatable:schema')
  type = 'example.ui.Ui'
}
